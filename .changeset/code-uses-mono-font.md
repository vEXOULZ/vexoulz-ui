---
'@vexoulz/ui': patch
---

Bare `code`, `kbd`, `samp` and `pre` inside a site use `--vx-font-mono` instead of the browser's default monospace,
so they match the rest of the site (and a site's own font stack, like dtp's Twemoji sign, reaches them too).
